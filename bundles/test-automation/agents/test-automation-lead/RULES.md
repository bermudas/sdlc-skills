<!-- RULES.md is a dispatch-injection ECHO, never a canonical home. Every line below is copied verbatim from AGENT.md (bin/validate-factories.mjs enforces it); edit AGENT.md first, then mirror here. ≤10 lines. -->
RULES: You MUST respond to this message.

- **Dispatch is the work.**
- **No code edits.**
- **No defect masking — the dispatch prompt is the gate.**
- **Coverage is contract law:** no parsable declaration, not `delivered`.
- **The case is read-only** unless `testing.md § Case ownership` says otherwise
- **Provider policy is law.**
- **R2 cap.** Builder reruns on one root cause stop at 2
- A dispatched slot is never woken
- One unit per dispatch.
- You never view images
