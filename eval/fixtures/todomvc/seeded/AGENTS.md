# AGENTS — TodoMVC fixture

Stack: vanilla JS, single `index.html`, no build. Package manager: npm.
Test framework: Playwright — install `npm install`, run `npx playwright test`.
Base branch: main. Locators expose `data-testid`.
