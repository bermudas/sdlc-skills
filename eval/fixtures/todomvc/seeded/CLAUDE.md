# TodoMVC fixture

Minimal vanilla-JS TodoMVC (single `index.html`, no build step). Package manager:
npm. Tests: Playwright (`npx playwright test`). Base branch: main. See `.agents/`
for the full team context.
